{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["npm run build:workers"]
    }
  },
  "deploy": {
    "startCommand": "npm run start:workers"
  }
}
