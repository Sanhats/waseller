# Servicio API (Nest). Opcional pero recomendado:
#   RAILPACK_CONFIG_FILE=infra/railpack/api.json
# Vaciá Custom Build Command en el panel si choca con Railpack.

[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build:api"

[deploy]
startCommand = "npm start"
