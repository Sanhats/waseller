# Copiar o fusionar en el servicio "API" del proyecto Railway (mismo repo).
# Build / start se ejecutan desde la raíz del monorepo.

[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build:api"

[deploy]
startCommand = "npm start"
