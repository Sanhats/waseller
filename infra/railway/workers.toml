# Servicio separado: workers (BullMQ). Mismo repo, otra instancia en Railway.

[build]
builder = "nixpacks"
# En Railway: Build Command = esto (compila). Nunca uses start:workers acá.
buildCommand = "npm ci && npm run build:workers"

[deploy]
startCommand = "npm run start:workers"
